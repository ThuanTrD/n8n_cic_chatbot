# Final Artifact Registry

All artifacts in `ARTIFACT_REGISTRY.md` are present and accepted. Runtime exports, synthetic test data, decrypted credentials and temporary scripts are excluded and removed after acceptance.
