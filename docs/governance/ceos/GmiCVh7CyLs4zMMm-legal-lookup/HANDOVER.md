# Handover

## Delivered
Legal/certificate lookup readiness with provenance, effect-status caveats, prompt-injection controls and deterministic abstention.

## Operator action
Supply documents using `LEGAL_DOCUMENT_CONTRACT.md`. At minimum provide exact title, document/reference number, issuer, immutable version, effective/expiry status, and page/article lineage.

## Rollback
Restore the prior workflow from the pre-change export/version or re-import the sanitized baseline represented by Git commit `479e87d`, then remap existing credentials if restoring from Git.

## Deferred
Dedicated immutable legal corpus/schema, ingestion worker, document approval workflow and real-document retrieval evaluation require the actual documents and governance choices.

