# Idea Comparison

1. Modify the old published export. This preserves flow-1 activation but loses newer flow-2 context/gateway work in the draft.
2. Modify the current combined draft, apply flow-1 context parity, and set webhook flags to flow 1 before publishing. Selected because both branches remain preserved and optimized.

Publishing uses n8n-supported offline CLI operations to avoid runtime registration drift.
