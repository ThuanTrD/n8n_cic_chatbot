# Review Report

A separate export confirmed current draft and published workflow exactly match the asserted build. Each flow graph has exactly one conversation-history node with limit 5 and no remaining history limit 8.

Production remains Webhook4. GET/POST registrations, n8n health, and Facebook challenge passed.

Review status: `accepted`.
