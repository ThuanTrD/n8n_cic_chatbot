# Preliminary Definition of Done

Success: container environment uses the requested domain, n8n returns healthy, the workflow remains active, GET and POST registrations remain present, the editor URL loads, and the production path remains reachable.

Failure: container health fails, registration changes, requested domain is unreachable, or secrets enter Git/log output.

Forbidden: modifying workflow logic, credentials, database contents, or unrelated services.
