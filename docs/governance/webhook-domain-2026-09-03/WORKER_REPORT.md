# Worker Report

Changed only `N8N_WEBHOOK_URL` and `N8N_EDITOR_BASE_URL` in `/opt/n8n/.env` from the obsolete quick-tunnel domain to `https://hydrogen-valium-liabilities-sit.trycloudflare.com/`.

A permission-600 backup was created at `/opt/n8n/.env.backup-webhook-domain-20260903T015911Z`. Only the `n8n` container was recreated with dependencies excluded. It returned to healthy state.
